export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000/api',
  serverUrl: 'http://localhost:3000',
  // Stripe publishable key (safe to expose in the browser). Use your TEST key here.
  stripePublishableKey: 'pk_test_REPLACE_ME',
};
